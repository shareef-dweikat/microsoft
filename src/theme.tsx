import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  // Theme configuration, including the 'mode' property
  palette: {
    mode: 'light', // 'light' or 'dark'
  },
  // Other theme settings
});

export default theme;